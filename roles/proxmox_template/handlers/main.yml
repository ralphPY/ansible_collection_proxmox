- name: Restart SSH
  delegate_to: "{{ template_ip }}"
  become: true
  ansible.builtin.systemd:
    name: ssh
    state: restarted
  # Optional: Add a delay to avoid locking yourself out
  # delay: 5